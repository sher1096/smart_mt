# 智慧医疗患者端（移动端）

基于 Vue 3 + TypeScript + Vite + Naive UI 开发的移动端患者应用。

## 技术栈

- **框架**: Vue 3.4+
- **语言**: TypeScript
- **构建工具**: Vite 5
- **UI 组件库**: Naive UI
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP 请求**: Axios

## 功能模块

### 核心功能
- ✅ 用户登录/注册
- ✅ 首页（新闻资讯、科室导航、推荐医生）
- ✅ 预约挂号（选择科室 → 选择医生 → 预约时段）
- ✅ 我的挂号（预约记录管理、取消预约）
- ✅ 我的病历
- ✅ 我的处方
- ✅ 我的体检
- ✅ 缴费中心
- ✅ 消息中心
- ✅ 个人中心

### 移动端特性
- 📱 移动端优先设计（max-width: 750px）
- 🎨 底部固定 TabBar 导航
- ⬅️ 顶部导航栏带返回按钮
- 🔄 页面过渡动画
- 🔐 路由守卫（登录状态检查）
- 🌐 API 请求自动添加 Bearer Token
- 🇨🇳 全中文界面

## 目录结构

```
patient/
├── src/
│   ├── api/              # API 接口模块
│   │   ├── auth.ts       # 认证相关
│   │   ├── department.ts # 科室相关
│   │   ├── doctor.ts     # 医生相关
│   │   ├── appointment.ts# 预约相关
│   │   └── index.ts      # 统一导出
│   ├── layouts/          # 布局组件
│   │   └── MobileLayout.vue # 移动端布局（TabBar）
│   ├── router/           # 路由配置
│   │   └── index.ts
│   ├── stores/           # 状态管理
│   │   └── user.ts       # 用户状态
│   ├── utils/            # 工具函数
│   │   └── request.ts    # Axios 封装
│   ├── views/            # 页面组件
│   │   ├── home/         # 首页
│   │   ├── login/        # 登录
│   │   ├── register/     # 注册
│   │   ├── department/   # 科室列表
│   │   ├── doctor/       # 医生列表
│   │   ├── appointment/  # 预约挂号
│   │   ├── medical/      # 病历
│   │   ├── prescription/ # 处方
│   │   ├── examination/  # 体检
│   │   ├── payment/      # 缴费
│   │   ├── message/      # 消息
│   │   └── profile/      # 个人中心
│   ├── App.vue           # 根组件
│   ├── AppContent.vue    # 内容包装组件
│   └── main.ts           # 入口文件
├── index.html            # HTML 模板
├── vite.config.ts        # Vite 配置
├── tsconfig.json         # TypeScript 配置
├── package.json          # 依赖配置
└── README.md             # 项目说明

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:5174

### 生产构建

```bash
npm run build
```

### 类型检查

```bash
npm run type-check
```

## 配置说明

### API 代理

项目配置了 API 代理，所有 `/api` 请求会被代理到 `http://localhost:3000`。

修改 `vite.config.ts` 中的 `server.proxy` 配置可以更改代理目标。

### 环境变量

`.env.development` - 开发环境配置

```env
VITE_APP_TITLE=智慧医疗患者端
VITE_APP_BASE_API=/api
VITE_APP_PORT=5174
```

## 路由配置

### 公开路由（无需登录）
- `/login` - 登录
- `/register` - 注册
- `/home` - 首页

### 受保护路由（需要登录）
- `/appointment/*` - 预约相关
- `/my-appointments` - 我的挂号
- `/medical-records` - 我的病历
- `/prescriptions` - 我的处方
- `/examinations` - 我的体检
- `/payment` - 缴费中心
- `/message` - 消息中心

## 状态管理

使用 Pinia 进行状态管理：

```typescript
// 用户状态
const userStore = useUserStore()

// 获取登录状态
userStore.isLoggedIn

// 获取用户信息
userStore.userInfo

// 登录
userStore.login(token, userInfo)

// 退出登录
userStore.logout()
```

## API 请求

所有 API 请求会自动：
- 添加 `Authorization: Bearer <token>` 请求头
- 处理 401 未授权（自动跳转登录）
- 统一错误处理

```typescript
import { login } from '@/api'

const res = await login({ username, password })
```

## 移动端适配

- 使用 `viewport` meta 标签进行移动端适配
- CSS 使用相对单位（rem、vw、vh）
- 底部 TabBar 适配安全区域
- 支持下拉刷新、上拉加载（可扩展）

## 浏览器兼容性

- Chrome/Edge (最新版)
- Safari (iOS 12+)
- Android WebView (Android 5+)

## 开发规范

- 使用 Composition API + `<script setup>` 语法糖
- 统一使用 TypeScript
- 组件名使用 PascalCase
- 文件名使用 kebab-case
- 遵循 Vue 3 官方风格指南

## 许可证

MIT License
```
